<div class="panel panel-default">
	<!-- Default panel contents -->
	<h2>Add Paypal transaction</h2>
	<!-- Form to manage workplace transaction -->
	<%= form_for :transaction, :url => {:action => 'register_paypal_transaction'}, :html => {:class => 'new_account', :role => 'form'} do |f| %>
		<%= render :partial => 'paypal_transaction_form', :locals => {:f => f, :transaction => @transaction} %>
		<%= f.submit "Register Workplace Transaction", :class => "form-control" %>
	<% end %>
</div>